<div class="form">
	<%= form_with url: "/create" do |f| %>
		<%= f.label :name, "名前" %><br>
		<%= f.text_field :name, required: true %><br>
		<%= f.label :times, "退出予定時間" %><br>
		<%= f.time_field :times, class: 'form-control'%><br><br>
		<%= f.submit "確定", class: "btn" %>
	<% end %>
</div>